services:
  nocodb:
    image: nocodb/nocodb:2026.05.1
    container_name: nocodb
    restart: always
    env_file:
      - .env
    volumes: 
      - "nocodb_data:/usr/app/data"
    networks:
      - internal_network
    deploy:
      resources:
        limits:
          memory: 1024M
          cpus: "0.5"

volumes:
  nocodb_data:
    driver: local

networks:
  internal_network:
    external: true